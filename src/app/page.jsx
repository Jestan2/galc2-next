//page.jsx
export const metadata = {
  title: "Great American Labor",
  description: "Moving, warehouse support, event setup/teardown, cleanouts, and construction cleanup. Book vetted labor-only crews with clear updates.",
};

import HomeClient from "./components/HomeClient";

export default function Page() {
  return <HomeClient />;
}